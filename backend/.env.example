# Browser origins allowed to call the AURA API
AURA_CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Local default: SQLite is used automatically when this is empty.
# Production: set a managed PostgreSQL connection string.
# AURA_DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/aura
